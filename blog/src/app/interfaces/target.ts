export interface Target {
    title?: string,
    text?:string,
    author?:string,
    img?:string,
    date?: Date,
    category?: string
}

// titulo, texto, autor, imagen (puede ser una url), fecha, categoria